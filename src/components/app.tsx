import { FunctionalComponent } from 'preact';
import Home from '../../src/routes/home';

const App: FunctionalComponent = () => {
    return (
        <div id="preact_root">
            <Home />
        </div>
    );
};

export default App;
